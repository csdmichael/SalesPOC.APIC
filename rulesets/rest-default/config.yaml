# Ruleset metadata – used by deploy scripts to route by API type
apiType: rest                       # rest | graphql | mcp
analyzerType: spectral              # analyzer engine used by API Center
analyzerConfigName: default         # deploy to the built-in default spectral config
description: >
  Full REST/OpenAPI governance – inlined Spectral OAS rules plus custom
  security-controls checks (rate limiting, input validation, error handling).
  Deployed into the default API Center analyzer configuration.
